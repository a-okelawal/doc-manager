describe('Document', function(){
  it(' should validate the creation of a new user document.', function(){

  });

  it(' should validate the return of all documents on Documents.all with query parameter limit.', function(){

  });

  it(' should employ the limit with an offset as well.', function(){

  });

  it(' should return all documents in order of their published dates.', function(){

  });
});
